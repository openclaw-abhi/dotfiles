-- ==========================================================================
--  NEOVIM CONFIGURATION (Neovim 0.11.5+ | Arch Linux)
-- ==========================================================================

-- 1. Load Settings (Options, Globals, UI)
require("settings")

-- 2. Load Auto-Commands (Events)
require("autocmds")

-- 3. Load Keymaps (Bindings & Terminal Logic)
require("keymaps")

-- 4. Load Plugins (Lazy.nvim)
require("plugins")
